#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int> v={39,9,81,45,90,27,72,18};
    for(int j=0;j<v.size()-1;j++){
        for(int i=0;i<v.size();i++){
            if(v[i]>v[i+1])
             swap(v[i],v[i+1]);
        }
    }
    for(int i=0;i<v.size();i++){
        cout<<v[i]<<" ";
    }
    return 0;
}
